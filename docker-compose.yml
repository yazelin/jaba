services:
  jaba:
    build: .
    container_name: jaba
    ports:
      - "8098:8098"
    volumes:
      - ./data:/app/data
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    restart: unless-stopped
