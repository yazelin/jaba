# Tasks

- [x] 建立 `user_prompt.md`，將 `jaba.json` 中的 `user_prompt` 內容轉換為 Markdown 格式
- [x] 建立 `manager_prompt.md`，將 `jaba.json` 中的 `manager_prompt` 內容轉換為 Markdown 格式
- [x] 修改 `app/data.py` 的 `get_jaba_prompt()` 函數，改為讀取 `.md` 檔案
- [x] 移除或簡化 `jaba.json`（刪除未使用的欄位）
