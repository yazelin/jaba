# Tasks

## 顯示尺寸與價格
- [x] 修改 `manager.html` 的 `updateStoresPanel()` 函數，當品項有 variants 時顯示各尺寸名稱和價格
- [x] 修改 `order.html` 的 `updateMenuPanel()` 函數，當品項有 variants 時顯示各尺寸，點擊時傳入對應的價格和尺寸

## 編輯功能
- [x] 在 `manager.html` 的店家列表中，每個店家加入「編輯」按鈕
- [x] 實作 `editStoreMenu(storeId)` 函數，載入現有菜單到編輯畫面
- [x] 修改編輯畫面，支援編輯現有菜單（不只是新辨識的菜單）
